# Optional. Without it the reviewer runs the offline rule-based extractor and
# every command below still works -- see the README on what you lose.
ANTHROPIC_API_KEY=

# Override the model used for extraction. Defaults to claude-opus-5.
ANTHROPIC_MODEL=claude-opus-5
